# LikeSized Claude Instructions

Before making any repository change, read and follow `AI_REPOSITORY_RULES.md` and `AGENTS.md`.

Non-negotiable: **No patches. Keep the repository fully clean and canonical.** Apply approved changes directly to the canonical source files. Do not create hotfix copies, `-fixed`, `-patched`, `-v2`, backup, temporary replacement, or parallel implementation files. Git history preserves prior states.
